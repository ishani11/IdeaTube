<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="AppTheme" >
        <item name="colorPrimary">@color/myThemeAccent</item>
    </style>

    <style name="JustTrying" parent="AppTheme" >
        <item name="colorPrimary">@color/myThemeAccent</item>
        <item name="colorPrimaryDark">@color/myThemePrimaryDark</item>
        <item name="colorAccent">@color/colorPrimaryDark</item>
        <item name="android:colorBackground">@color/MyThemebackground</item>
    </style>

</resources>
